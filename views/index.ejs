<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Students</title>
   <link rel="stylesheet" href=" https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"> 
</head>
<body >
    <a href="/new" class="card-link btn btn-sm btn-success">Add New Student</a>
    <% if (students && students.length>0) { %>
        <h2>Students</h2>
     
        <section class="d-flex flex-wrap  justify-content-center align-items-center gap-3" >
            <% for( let student of students ) { %>
                <div class="card align-items-center" style="border: 2px solid black;">
                    
                    <h3 class="card-title">Name: <%=student.name %></h3>
                    <!-- <br> -->
                    <strong> Email: <%= student.email %></strong>
                    <!-- <br> -->
                    <h4 class="card-subtitle"> Age: <%=student.age %></h4> 
                    <!-- <br> -->
                    <strong>year: <%= student.year %></strong>
                    <!-- <br> -->
                    <strong> Grade: <%= student.grade %></strong>
                    <strong> Grade: <%= student.remark %></strong>
                    <section>
                        
                        <a  href="/students/<%= student._id%>/edit" class="card-link btn btn-sm btn btn-primary">Edit Data</a>
                        <a href="/students/<%= student._id%>/open" class="btn btn-sm btn-primary">View</a>
                        <a href="/students/<%= student._id%>" class="btn btn-sm btn-danger">Delete</a>
                    </section>
                </div>
                <% } %>
                <% } else { %>
                 <p>No Student Found</p>
                <% } %>
        </section>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js" integrity="sha384-0pUGZvbkm6XF6gxjEnlmuGrJXVbNuzT9qBBavbLwCsOGabYfZo0T0to5eqruptLy" crossorigin="anonymous"></script>
    
</body>
</html>